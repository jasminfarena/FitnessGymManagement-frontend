# FitnessGymManagement-backend

Fitness Management System that is used to register new gym members and update existing members.


## 🚀 Tech Stack

- Frontend: ReactJS
- Environment: dotenv
- Other tools: Postman / Thunder Client for API testing

## 🛠️ Configure Environment Variables (.env)

- Open the .env file.
- Update the backend api as -hostname/api (REACT_APP_API_BASE).
- Change the port number if needed.


## Start the server

  1. Install Dependencies
       ```bash
     npm install
     ```
  2. Run the Frontend
     ```bash
     npm run start
     ```

## Login Credentials

Use the following credentials to log in:

- **Username:** `emma@gmail.com`
- **Password:** `123456`

## Application Features
- After login, you will be redirected to the Dashboard, which shows a list of Members where user view and update.
- On the right side, there's a dropdown where Admin can register Staff and Members, while staff can only register members.

